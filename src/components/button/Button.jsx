import React from "react";

const Button = (type) => {
  return (
    <button
      type={onsubmit}
      className="
      relative 
      disabled:opacity-70 
      disabled:cursor-not-allowed 
      rounded-lg 
      hover:opacity-80 
      transition 
      w-full
      text-center
      bg-rose-500
      border-black
      py-1
      font-light
      text-sm
      "
    >
      button
    </button>
  );
};

export default Button;
